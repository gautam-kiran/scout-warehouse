<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>ch.scout.warehouse</groupId>
    <artifactId>scout.warehouse-root</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>scout.warehouse</artifactId>
  <packaging>pom</packaging>

  <properties>
    <ch.scout.warehouse.scout.warehouse.version>${project.version}</ch.scout.warehouse.scout.warehouse.version>
    <org.eclipse.scout.rt.version>26.1.7</org.eclipse.scout.rt.version>
    <master_npm_release_dependency_mapping>--mapping.0.regex=@eclipse-scout --mapping.0.version=${org.eclipse.scout.rt.version}</master_npm_release_dependency_mapping>

    <!-- docker image build settings -->
    <!-- build target (push) -->
    <docker.app.image.registry>localhost:5000</docker.app.image.registry> <!-- set a desired registry before using profile exec.docker.image (e.g. registry.hub.docker.com/yourusername) -->
    <docker.app.image.tag>1.0.0-latest</docker.app.image.tag>
    <!-- java source image (pull) -->
    <docker.java.image.name>eclipse-temurin</docker.java.image.name>
    <docker.java.image.tag>25-jdk-noble</docker.java.image.tag>
    <docker.java.image>${docker.java.image.name}:${docker.java.image.tag}</docker.java.image>
  </properties>

  <modules>
    <module>../scout.warehouse.shared</module>
    <module>../scout.warehouse.server</module>
    <module>../scout.warehouse.client</module>
    <module>../scout.warehouse.server.app</module>
    <module>../scout.warehouse.server.app.dev</module>
    <module>../scout.warehouse.server.app.image</module>
    <module>../scout.warehouse.ui.html</module>
    <module>../scout.warehouse.ui.html.app</module>
    <module>../scout.warehouse.ui.html.app.dev</module>
    <module>../scout.warehouse.ui.html.app.image</module>
  </modules>

  <profiles>
    <profile>
      <!-- required for .app.image module to apply filtering on config.properties -->
      <id>filter.config.property</id>
      <activation>
        <file>
          <exists>${basedir}${file.separator}src${file.separator}main${file.separator}resources${file.separator}config.properties</exists>
        </file>
      </activation>
      <build>
        <resources>
          <resource>
            <directory>${project.basedir}${file.separator}src${file.separator}main${file.separator}resources</directory>
            <filtering>true</filtering>
          </resource>
        </resources>
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>properties-maven-plugin</artifactId>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <!--  set property docker.app.image.registry before activating profile -->
      <id>exec.docker.image</id>
      <build>
        <pluginManagement>
          <plugins>
            <plugin>
              <!-- phase binding required for .app.image module to build and publish docker image -->
              <groupId>com.google.cloud.tools</groupId>
              <artifactId>jib-maven-plugin</artifactId>
              <executions>
                <execution>
                  <phase>package</phase>
                  <goals>
                    <goal>build</goal>
                  </goals>
                </execution>
              </executions>
            </plugin>
          </plugins>
        </pluginManagement>
      </build>
    </profile>
  </profiles>

  <dependencyManagement>
    <dependencies>
      <!-- Scout Dependencies -->
      <dependency>
        <groupId>org.eclipse.scout.rt</groupId>
        <artifactId>org.eclipse.scout.rt</artifactId>
        <version>${org.eclipse.scout.rt.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- Application Dependencies -->
      <dependency>
        <groupId>ch.scout.warehouse</groupId>
        <artifactId>scout.warehouse.client</artifactId>
        <version>${ch.scout.warehouse.scout.warehouse.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.scout.warehouse</groupId>
        <artifactId>scout.warehouse.shared</artifactId>
        <version>${ch.scout.warehouse.scout.warehouse.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.scout.warehouse</groupId>
        <artifactId>scout.warehouse.server</artifactId>
        <version>${ch.scout.warehouse.scout.warehouse.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.scout.warehouse</groupId>
        <artifactId>scout.warehouse.server.app</artifactId>
        <version>${ch.scout.warehouse.scout.warehouse.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.scout.warehouse</groupId>
        <artifactId>scout.warehouse.server.app.dev</artifactId>
        <version>${ch.scout.warehouse.scout.warehouse.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.scout.warehouse</groupId>
        <artifactId>scout.warehouse.server.app.image</artifactId>
        <version>${ch.scout.warehouse.scout.warehouse.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.scout.warehouse</groupId>
        <artifactId>scout.warehouse.server.app.zip</artifactId>
        <version>${ch.scout.warehouse.scout.warehouse.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.scout.warehouse</groupId>
        <artifactId>scout.warehouse.ui.html</artifactId>
        <version>${ch.scout.warehouse.scout.warehouse.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.scout.warehouse</groupId>
        <artifactId>scout.warehouse.ui.html.app</artifactId>
        <version>${ch.scout.warehouse.scout.warehouse.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.scout.warehouse</groupId>
        <artifactId>scout.warehouse.ui.html.app.dev</artifactId>
        <version>${ch.scout.warehouse.scout.warehouse.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.scout.warehouse</groupId>
        <artifactId>scout.warehouse.ui.html.app.image</artifactId>
        <version>${ch.scout.warehouse.scout.warehouse.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.scout.warehouse</groupId>
        <artifactId>scout.warehouse.ui.html.app.zip</artifactId>
        <version>${ch.scout.warehouse.scout.warehouse.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>
</project>
